# 派巢猫爬架批发独立站 · 使用说明

你好！这是一个纯静态网站（只有 HTML/CSS/JS，无需服务器、无需数据库），
可以直接双击 `index.html` 在浏览器打开预览，也能免费部署到 GitHub Pages 上线。

---

## 一、5 个页面

| 文件 | 页面 | 说明 |
|---|---|---|
| `index.html` | 首页 | 品牌介绍、热销产品、阶梯价、合作流程 |
| `products.html` | 产品目录 | 全部 8 个产品（NO.1~NO.8），可按大款/小款筛选 |
| `about.html` | 关于我们 | 工厂参数、品控、OEM、常见问题 |
| `contact.html` | 联系询盘 | 询盘表单 + WhatsApp + PayPal 收款 |
| `terms.html` | 条款与隐私 | 交易条款、售后、隐私政策 |

---

## 二、双语是怎么工作的（重要）

- 网站会**自动检测访客的浏览器语言**：
  - 浏览器是中文 → 显示**中文**
  - 其他任何语言（国外客户）→ 自动显示**英文**
- 访客也可以点右上角的「中文 / EN」按钮手动切换，选择会被记住。
- 你想强制看某种语言，可在网址后加参数：`index.html?lang=en` 或 `?lang=zh`。

---

## 三、换成你自己的信息（只改 1 个文件）

打开 **`assets/js/config.js`**，把带 ★ 的项改成你的真实信息，全站自动生效。最关键的几项：

```js
whatsapp: "8613800000000",   // ★ 改成你的 WhatsApp 号：国码+号码，纯数字
                             //   例：中国 138xxxx8888 → "86138xxxx8888"
email: "sales@pawnest-example.com",  // ★ 改成你能收到邮件的邮箱（收询盘用）
phone: "+86 138 0000 0000",          // ★ 电话
```

**PayPal 说明（请注意）**：
你给的是 PayPal **邮箱** `hoangkyshsj67@gmail.com`。
`paypal.me/` 后面要接的是 PayPal **用户名**，不是邮箱，所以直接用邮箱拼链接是打不开的。两种做法：

- **做法 A（推荐）**：登录 https://paypal.me 免费设置一个用户名（例如 `pawnest`），
  然后在 `config.js` 里改：
  ```js
  paypalMe: "https://paypal.me/pawnest",
  paypalMeIsValid: true,   // 改成 true
  ```
- **做法 B（暂不设置）**：保持 `paypalMeIsValid: false`。
  这时「用 PayPal 付款」按钮会跳到 PayPal 官方转账页，联系页也会显示你的收款邮箱
  `hoangkyshsj67@gmail.com` 和详细付款步骤，客户照样能把钱付到你的账户。

---

## 四、换产品图片（不用改代码）

1. 准备 8 张产品照片，**建议正方形**（例如 800×800 像素），JPG 格式。
2. 重命名为 `no1.jpg`、`no2.jpg` …… `no8.jpg`，编号要和产品对应。
3. 放进文件夹：`assets/images/products/`
4. 刷新网页即可显示。（放进去之前显示的是占位示意图，不影响功能）

> 如果你的图是 PNG 格式：打开 `assets/js/products.js`，把对应产品的
> `image: "...no1.jpg"` 后缀改成 `.png` 即可。

**首页大图**（可选）：把一张工厂/产品横图命名为 `hero.jpg` 放进
`assets/images/site/`，首页顶部就会用它。

---

## 五、改产品文字 / 价格 / 加减产品

打开 **`assets/js/products.js`**，里面每个产品是一段 `{ ... }`：
- 改文字：直接改 `nameZh`（中文名）、`nameEn`（英文名）、`size`、`basePrice`（200件基础单价）等。
- 加产品：复制一整段，改 `code` 为 `"NO.9"` 并往后递增。
- 删产品：删掉整段。

**改批发规则 / 折扣**：在 `assets/js/config.js` 的 `tiers` 里改。
目前设置：200-499 件原价，500-999 件 9 折，1000 件以上 8.5 折。

---

## 六、让询盘直接进邮箱（可选，推荐）

默认情况下，客户点「提交询盘」会打开他自己的邮件软件（已自动填好内容）。
如果想让询盘**直接发到你邮箱**（客户体验更好），5 分钟搞定：

1. 打开 https://formspree.io 用邮箱免费注册。
2. 新建一个 form，拿到表单 ID（形如 `xldeqvpk`）。
3. 在 `assets/js/config.js` 里改：
   ```js
   formMode: "formspree",
   formspreeId: "你的表单ID",
   ```

---

## 七、上线到 GitHub（免费）

详见同目录下的 **`GitHub上线教程.md`**，一步步照做即可，全程免费。

---

有任何要调整的，把这个文件夹发给我，告诉我你想改什么就行。
